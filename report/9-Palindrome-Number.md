# 回文串
简直送分题，不过题目说要no extra space，即不能用空间，没有尝试用split-> revese -> join。甚至连toString都不敢用...（其实完全可以）

所以思路就是，每次消耗头尾两位。
计算出原来数字长度len = Math.log10

1. 取尾位，%10即可
2. 取头位，我用的是当前数除以10^len
3. 比较
4. 当前数 = (当前数 - 头位 * 10^len) / 10


